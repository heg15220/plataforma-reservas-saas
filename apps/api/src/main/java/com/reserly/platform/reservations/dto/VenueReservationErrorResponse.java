package com.reserly.platform.reservations.dto;

/** Error estable del panel privado que no expone detalles de persistencia ni propiedad. */
public record VenueReservationErrorResponse(String code) {}
